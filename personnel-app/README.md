# 🏢 تطبيق لوحة الموظفين - Personnel Panel App

تطبيق سطح المكتب المستقل لإدارة الموظفين والحضور باستخدام Electron + React مع تخزين محلي في SQLite.

## ✨ المميزات

- 🖥️ **تطبيق سطح مكتب مستقل** - يعمل بدون إنترنت
- 💾 **تخزين محلي** - جميع البيانات محفوظة على الحاسوب باستخدام SQLite
- ⏰ **إدارة الحضور** - تسجيل حضور ومغادرة الموظفين
- 👥 **إدارة المستفيدين** - متابعة المستفيدين والخدمات
- 📢 **الإعلانات** - نظام إعلانات داخلي
- 🌙 **واجهة عصرية** - تصميم احترافي ومريح للعين
- 🔐 **آمن** - بيانات محلية بالكامل

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js (v18 أو أحدث)
- npm أو yarn

### خطوات التثبيت

1. **تثبيت المكتبات:**
```bash
cd personnel-app
npm install
```

2. **تشغيل التطبيق في وضع التطوير:**
```bash
npm run dev
```

3. **بناء التطبيق للإنتاج (.exe):**
```bash
npm run build
npm run package
```

سيتم إنشاء ملف التثبيت في مجلد `dist-electron/`

## 📦 البنية

```
personnel-app/
├── electron/              # ملفات Electron الرئيسية
│   ├── main.js           # العملية الرئيسية + SQLite
│   └── preload.js        # الجسر الآمن للواجهة
├── src/                  # كود React
│   ├── pages/           # صفحات التطبيق
│   ├── services/        # API adapter
│   └── i18n.js          # الترجمة (عربي/فرنسي)
├── package.json
└── vite.config.js
```

## 🔑 بيانات الدخول الافتراضية

```
اسم المستخدم: admin
كلمة المرور: admin123
```

## 💾 قاعدة البيانات

يتم تخزين قاعدة البيانات في:
```
Windows: C:\Users\[username]\AppData\Roaming\personnel-app\personnel.db
```

### الجداول:
- `users` - بيانات المستخدمين والموظفين
- `attendance` - سجلات الحضور والانصراف
- `beneficiaries` - بيانات المستفيدين
- `announcements` - الإعلانات

## 🛠️ التطوير

### تشغيل Vite فقط:
```bash
npm run dev
```

### تشغيل Electron مع Vite:
```bash
npm run dev
# في terminal آخر:
npm run electron:dev
```

## 📱 الصفحات المتوفرة

1. **تسجيل الدخول** - `/login`
2. **لوحة التحكم** - `/dashboard`
3. **الحضور** - `/attendance`
4. **المستفيدين** - `/beneficiaries`
5. **الإعلانات** - `/announcements`

## 🔧 البرمجة

### إضافة API جديد:

1. في `electron/main.js`:
```javascript
ipcMain.handle('myFeature:action', async (event, data) => {
  // منطق قاعدة البيانات
  return { success: true, data: result }
})
```

2. في `electron/preload.js`:
```javascript
myFeatureAction: (data) => ipcRenderer.invoke('myFeature:action', data)
```

3. في `src/services/api.js`:
```javascript
export const api = {
  myFeatureAction: async (data) => {
    return await window.electronAPI.myFeatureAction(data)
  }
}
```

## 📄 License

MIT

## 🤝 المساهمة

مرحباً بجميع المساهمات لتحسين التطبيق!
